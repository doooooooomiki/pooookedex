---

---

<poke-display poke-src="">
    <div class="screen">
      <div class="output">
        <img src="" alt="">
      </div>
      <poke-input poke-value=""></poke-input>
    </div>
</poke-display>

<script>
   class PokeDisplay extends HTMLElement {
    constructor() {
      super();
    }

    get img() {
      return this.querySelector('img');
    }

    static get observedAttributes() { 
      return ['poke-src'];
    }

    attributeChangedCallback(name, oldValue, newValue) {
      this.img.setAttribute('src', newValue);
    }
  }
  customElements.define("poke-display", PokeDisplay);

  class PokeInput extends HTMLElement {
    constructor() {
      super();
    }

    static get observedAttributes() { 
      return ['poke-value'];
    }

    attributeChangedCallback(name, oldValue, newValue) {
      this.innerText = newValue;
    }

  }
  customElements.define("poke-input", PokeInput);
</script>